<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.mapper.UserInfoMapper">
    <select id="LoginCheckRole" parameterType="com.qf.vo.LoginCheckVo" resultType="com.qf.pojo.UserInfo" >
          select u.* from userInfo u,user_role ur where u.username=#{username} and u.password=#{password} and u.userInfoId=ur.userInfoId and u.flag=1 and ur.flag=1
    </select>
</mapper>